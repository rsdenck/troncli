# Binaries
*.exe
*.dll
*.so
*.dylib
bin/
dist/
troncli.txt
troncli

# IDEs
.vscode/
.idea/
.trae/
*.swp

# Go
vendor/
go.work
go.work.sum

# OS
.DS_Store
Thumbs.db

# Test artifacts
*.out
coverage*

# Temporary binaries
# audit (removed to avoid ignoring module dir)
# process (removed to avoid ignoring module dir)
# scheduler (removed to avoid ignoring module dir)
# service (removed to avoid ignoring module dir)
