# Bash

Execute Bash commands

## Usage#

```
nux bash <command>
```

## Description#

Executes bash commands directly through NUX CLI with proper escaping and output formatting.

## Examples#

List files:
nux bash "ls -la"

Check disk usage:
nux bash "df -h"
